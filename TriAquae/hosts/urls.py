from django.conf.urls import patterns, url
from TriAquae.hosts import views

urlpatterns = patterns('',
    #url(r'^assets$', views.assets, name='assets'),
    #url(r'^(?P<hosts_name>host\d+)/$', views.detail, name='detail'),
    #url(r'^status_detail$', views.status_detail, name='status_detail'),
    #url(r'^diff$', views.diff, name='diff'),
    #url(r'^status$', views.status, name='status'),
    #url(r'^hardware$', views.HardwareInfo),

)
